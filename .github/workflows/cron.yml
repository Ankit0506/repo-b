name: Daily Morning Workflow

on:
  schedule:
    # Cron syntax to run every minute
    - cron: '* * * * *'

jobs:
  daily-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Daily Task
        run: |
          # Your specific task commands here
          echo "Running daily workflow"
