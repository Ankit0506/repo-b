name: Daily Morning Workflow

on:
  schedule:
    # Cron syntax for 7 AM UTC daily
    - cron: '0 14 41 * *'

jobs:
  daily-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Daily Task
        run: |
          # Your specific task commands here
          echo "Running daily morning workflow"
